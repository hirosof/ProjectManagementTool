# pmtool チュートリアル

**バージョン:** 1.0.0（Phase 4 完了版）
**最終更新:** 2026-01-20

---

## 目次

1. [はじめに](#1-はじめに)
2. [シナリオ1: 基本的なプロジェクト管理](#2-シナリオ1-基本的なプロジェクト管理)
   - 2.1 [プロジェクト作成](#21-プロジェクト作成)
   - 2.2 [SubProjectとTask作成](#22-subprojectとtask作成)
   - 2.3 [SubTask作成](#23-subtask作成)
   - 2.4 [階層ツリー表示](#24-階層ツリー表示)
   - 2.5 [ステータス更新](#25-ステータス更新)
   - 2.6 [エンティティの更新](#26-エンティティの更新)
   - 2.7 [削除操作](#27-削除操作)
3. [シナリオ2: 依存関係管理](#3-シナリオ2-依存関係管理)
   - 3.1 [依存関係の追加](#31-依存関係の追加)
   - 3.2 [DONE遷移条件の確認](#32-done遷移条件の確認)
   - 3.3 [依存関係の可視化](#33-依存関係の可視化)
   - 3.4 [橋渡し削除の実践](#34-橋渡し削除の実践)
4. [まとめ](#4-まとめ)

---

## 1. はじめに

このチュートリアルでは、pmtool を使った実践的なプロジェクト管理の方法を、2つのシナリオを通じて学びます。

**チュートリアルのゴール:**
プロジェクト作成 → 依存関係設定 → ステータス遷移 → 安全な削除という一連の典型的な利用フローを体験します。これにより、pmtool の基本的な操作方法を習得し、実際のプロジェクト管理に活用できるようになります。

**シナリオ1: 基本的なプロジェクト管理**
- プロジェクト・SubProject・Task・SubTask の作成
- ステータス更新
- 削除操作

**シナリオ2: 依存関係管理**
- 依存関係の追加
- DONE遷移条件の理解
- 依存関係の可視化
- 橋渡し削除

**前提条件:**
- pmtool がインストール済み（詳細は USER_GUIDE.md の「2. インストール」を参照）
- データベースが初期化済み

それでは、実践的なシナリオで pmtool の使い方を学びましょう。

---

## 2. シナリオ1: 基本的なプロジェクト管理

**シナリオ概要:**
「Webアプリケーション開発」プロジェクトを作成し、フロントエンド開発とバックエンド開発の2つのSubProjectに分けて管理します。

### 2.1 プロジェクト作成

まず、プロジェクトを作成します。

```bash
$ pmtool add project --name "Webアプリケーション開発" --desc "ECサイトの開発プロジェクト"
✓ Project created: Webアプリケーション開発 (ID: 1)
```

プロジェクト一覧を確認します。

```bash
$ pmtool list projects

Projects
┏━━━━┳━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┳━━━━━━━━━━━━━━━━━┳━━━━━━━━━━━━━━━━━━━━━┓
┃ ID ┃ Name                        ┃ Created         ┃ Updated             ┃
┡━━━━╇━━━━━━━━━━━━━━━━━━━━━━━━━━━━━╇━━━━━━━━━━━━━━━━━╇━━━━━━━━━━━━━━━━━━━━━┩
│ 1  │ Webアプリケーション開発     │ 2026-01-20      │ 2026-01-20          │
└────┴─────────────────────────────┴─────────────────┴─────────────────────┘
```

### 2.2 SubProjectとTask作成

次に、「フロントエンド開発」と「バックエンド開発」のSubProjectを作成します。

```bash
# フロントエンド開発 SubProject
$ pmtool add subproject --project 1 --name "フロントエンド開発" --desc "React/TypeScriptによるフロント実装"
✓ SubProject created: フロントエンド開発 (ID: 1)

# バックエンド開発 SubProject
$ pmtool add subproject --project 1 --name "バックエンド開発" --desc "Node.js/ExpressによるAPI実装"
✓ SubProject created: バックエンド開発 (ID: 2)
```

フロントエンド開発のTaskを作成します。

```bash
# ログイン画面実装 Task
$ pmtool add task --subproject 1 --name "ログイン画面実装" --desc "ユーザー認証画面の実装"
✓ Task created: ログイン画面実装 (ID: 1)

# ダッシュボード実装 Task
$ pmtool add task --subproject 1 --name "ダッシュボード実装" --desc "メインダッシュボード画面の実装"
✓ Task created: ダッシュボード実装 (ID: 2)
```

バックエンド開発のTaskを作成します。

```bash
# 認証API実装 Task
$ pmtool add task --subproject 2 --name "認証API実装" --desc "JWT認証の実装"
✓ Task created: 認証API実装 (ID: 3)

# データAPI実装 Task
$ pmtool add task --subproject 2 --name "データAPI実装" --desc "CRUD APIの実装"
✓ Task created: データAPI実装 (ID: 4)
```

Project直下のTaskも作成してみます。

```bash
# プロジェクト計画 Task（Project直下）
$ pmtool add task --project 1 --name "プロジェクト計画" --desc "要件定義と設計"
✓ Task created: プロジェクト計画 (ID: 5)
```

### 2.3 SubTask作成

「ログイン画面実装」タスクのSubTaskを作成します。

```bash
# 画面設計 SubTask
$ pmtool add subtask --task 1 --name "画面設計" --desc "UIモックアップ作成"
✓ SubTask created: 画面設計 (ID: 1)

# 実装 SubTask
$ pmtool add subtask --task 1 --name "実装" --desc "React componentの実装"
✓ SubTask created: 実装 (ID: 2)

# テスト SubTask
$ pmtool add subtask --task 1 --name "テスト" --desc "単体テスト・E2Eテスト"
✓ SubTask created: テスト (ID: 3)
```

「認証API実装」タスクのSubTaskも作成します。

```bash
$ pmtool add subtask --task 3 --name "JWT認証実装"
✓ SubTask created: JWT認証実装 (ID: 4)

$ pmtool add subtask --task 3 --name "ミドルウェア実装"
✓ SubTask created: ミドルウェア実装 (ID: 5)

$ pmtool add subtask --task 3 --name "テスト"
✓ SubTask created: テスト (ID: 6)
```

### 2.4 階層ツリー表示

プロジェクト全体の構造を確認します。

```bash
$ pmtool show project 1

📦 Webアプリケーション開発 (ID: 1)
├── 📁 フロントエンド開発 (ID: 1)
│   ├── 📝 ログイン画面実装 (ID: 1) [ ] UNSET
│   │   ├── ✏️ 画面設計 (ID: 1) [ ] UNSET
│   │   ├── ✏️ 実装 (ID: 2) [ ] UNSET
│   │   └── ✏️ テスト (ID: 3) [ ] UNSET
│   └── 📝 ダッシュボード実装 (ID: 2) [ ] UNSET
├── 📁 バックエンド開発 (ID: 2)
│   ├── 📝 認証API実装 (ID: 3) [ ] UNSET
│   │   ├── ✏️ JWT認証実装 (ID: 4) [ ] UNSET
│   │   ├── ✏️ ミドルウェア実装 (ID: 5) [ ] UNSET
│   │   └── ✏️ テスト (ID: 6) [ ] UNSET
│   └── 📝 データAPI実装 (ID: 4) [ ] UNSET
└── 📝 (Project直下のTask)
    └── 📝 プロジェクト計画 (ID: 5) [ ] UNSET
```

### 2.5 ステータス更新

作業を進めていきます。まず「プロジェクト計画」を完了させます。

```bash
# プロジェクト計画を NOT_STARTED に変更
$ pmtool status task 5 NOT_STARTED
✓ Status updated: Task 5 → NOT_STARTED

# プロジェクト計画を IN_PROGRESS に変更
$ pmtool status task 5 IN_PROGRESS
✓ Status updated: Task 5 → IN_PROGRESS

# プロジェクト計画を DONE に変更（SubTaskがないため成功）
$ pmtool status task 5 DONE
✓ Status updated: Task 5 → DONE
```

「ログイン画面実装」の作業を進めます。

```bash
# SubTask「画面設計」を IN_PROGRESS に変更
$ pmtool status subtask 1 IN_PROGRESS
✓ Status updated: SubTask 1 → IN_PROGRESS

# SubTask「画面設計」を DONE に変更
$ pmtool status subtask 1 DONE
✓ Status updated: SubTask 1 → DONE

# SubTask「実装」を IN_PROGRESS に変更
$ pmtool status subtask 2 IN_PROGRESS
✓ Status updated: SubTask 2 → IN_PROGRESS

# SubTask「実装」を DONE に変更
$ pmtool status subtask 2 DONE
✓ Status updated: SubTask 2 → DONE

# SubTask「テスト」を DONE に変更
$ pmtool status subtask 3 DONE
✓ Status updated: SubTask 3 → DONE
```

すべてのSubTaskが完了したので、Task「ログイン画面実装」を DONE にできます。

```bash
# Task「ログイン画面実装」を DONE に変更
$ pmtool status task 1 DONE
✓ Status updated: Task 1 → DONE
```

ここで、SubTaskがまだ完了していないTaskをDONEにしようとするとエラーになります。

```bash
# Task「認証API実装」を DONE にしようとする（SubTaskが未完了）
$ pmtool status task 3 DONE
❌ Error: Cannot transition to DONE
  Reason: Child SubTask 4 is not DONE (current: UNSET)
  Hint: Complete all child SubTasks before marking this task as DONE
```

**なぜ失敗するか:** DONE遷移条件（すべての子SubTaskがDONE）を満たしていないため。詳細は [USER_GUIDE.md の「3.2 ステータス管理」](USER_GUIDE.md#32-ステータス管理) を参照してください。

### 2.6 エンティティの更新

エンティティの名前や説明を更新できます。

```bash
# Task の名前を変更
$ pmtool update task 2 --name "ダッシュボード実装（改訂版）"
✓ Task updated: ダッシュボード実装（改訂版）

# SubProject の説明を更新
$ pmtool update subproject 1 --desc "React/TypeScript + Tailwind CSS による実装"
✓ SubProject updated
```

### 2.7 削除操作

不要になったエンティティを削除します。

```bash
# SubTask「テスト」（ID: 3）を削除
$ pmtool delete subtask 3
⚠️  Are you sure you want to delete SubTask 3: テスト? [y/N]: y
✓ SubTask deleted (ID: 3)
```

子が存在するエンティティを削除しようとするとエラーになります。

```bash
# Task「認証API実装」を削除しようとする（SubTaskが存在）
$ pmtool delete task 3
❌ Error: Cannot delete Task 3: child SubTasks exist
  Hint: Use --bridge to reconnect dependencies, or --cascade --force to delete the entire subtree
```

**なぜ失敗するか:** 子エンティティ（SubTask）が存在するため、通常削除では削除できません。詳細は [USER_GUIDE.md の「5. 削除操作の詳細」](USER_GUIDE.md#5-削除操作の詳細) を参照してください。

連鎖削除を使うと、子も含めて削除できます。

```bash
# Task「認証API実装」を連鎖削除（dry-run で影響範囲を確認）
$ pmtool delete task 3 --cascade --dry-run

[DRY RUN] Would delete:
  - Task 3: 認証API実装
  - 3 SubTasks: JWT認証実装, ミドルウェア実装, テスト

# 実際に削除
$ pmtool delete task 3 --cascade --force

⚠️  WARNING: This will delete the following entities:
  - Task 3: 認証API実装
  - 3 SubTasks

Proceed? [y/N]: y
✓ Task and subtree deleted (ID: 3)
```

---

## 3. シナリオ2: 依存関係管理

**シナリオ概要:**
タスク間に依存関係を設定し、DONE遷移条件を理解します。また、依存関係の可視化と橋渡し削除を実践します。

**前提:**
シナリオ1で作成したプロジェクトを引き続き使用します。

### 3.1 依存関係の追加

まず、新しいTaskを追加します。

```bash
# フロントエンド開発に「商品一覧画面実装」を追加
$ pmtool add task --subproject 1 --name "商品一覧画面実装"
✓ Task created: 商品一覧画面実装 (ID: 6)

# バックエンド開発に「商品API実装」を追加
$ pmtool add task --subproject 2 --name "商品API実装"
✓ Task created: 商品API実装 (ID: 7)

# フロントエンド開発に「商品詳細画面実装」を追加
$ pmtool add task --subproject 1 --name "商品詳細画面実装"
✓ Task created: 商品詳細画面実装 (ID: 8)
```

依存関係を追加します。

```bash
# 商品API実装 → 商品一覧画面実装（APIが完成しないとフロント実装できない）
$ pmtool deps add task --from 7 --to 6
✓ Dependency added: Task 7 → Task 6

# 商品一覧画面実装 → 商品詳細画面実装（一覧画面が完成してから詳細画面を実装）
$ pmtool deps add task --from 6 --to 8
✓ Dependency added: Task 6 → Task 8
```

依存関係チェーンができました: **Task 7 → Task 6 → Task 8**

サイクル（循環）を作ろうとするとエラーになります。

```bash
# Task 8 → Task 7 の依存関係を追加しようとする（サイクルが発生）
$ pmtool deps add task --from 8 --to 7
❌ Error: Cycle detected: 8 → 7 → 6 → 8
  → Cannot add this dependency (would create a cycle)
```

**なぜ失敗するか:** 依存関係がサイクル（循環）を形成してしまうため、DAG（有向非循環グラフ）制約に違反します。詳細は [USER_GUIDE.md の「3.3 依存関係管理」](USER_GUIDE.md#33-依存関係管理) を参照してください。

### 3.2 DONE遷移条件の確認

依存関係がある場合、先行ノードが DONE でないと後続ノードを DONE にできません。

```bash
# Task 6（商品一覧画面実装）を DONE にしようとする（先行ノードが未完了）
$ pmtool status task 6 DONE
❌ Error: Cannot transition to DONE
  Reason: Predecessor Task 7 is not DONE (current: UNSET)
  Hint: Complete all predecessor tasks before marking this task as DONE
```

**なぜ失敗するか:** DONE遷移条件（すべての先行ノードがDONE）を満たしていないため。先行ノードを先に完了させる必要があります。

dry-run で遷移可否をチェックできます。

```bash
# Task 6 の DONE 遷移をチェック
$ pmtool status task 6 DONE --dry-run

[DRY RUN] Cannot transition to DONE:
  - All predecessors are DONE: ❌ (Task 7 is UNSET)
  - All child SubTasks are DONE: ✓ (no child SubTasks)
  → Transition is NOT allowed
```

Task 7 を先に DONE にしてから、Task 6 を DONE にします。

```bash
# Task 7 を DONE に変更
$ pmtool status task 7 DONE
✓ Status updated: Task 7 → DONE

# Task 6 を DONE に変更（今度は成功）
$ pmtool status task 6 DONE
✓ Status updated: Task 6 → DONE

# Task 8 を DONE に変更（先行ノードが DONE なので成功）
$ pmtool status task 8 DONE
✓ Status updated: Task 8 → DONE
```

### 3.3 依存関係の可視化

#### 3.3.1 deps list（依存関係一覧）

```bash
$ pmtool deps list task 6

Dependencies for Task 6: 商品一覧画面実装

Predecessors (先行ノード):
  Task 7: 商品API実装 [✓] DONE (Project: Webアプリケーション開発, SubProject: バックエンド開発)

Successors (後続ノード):
  Task 8: 商品詳細画面実装 [✓] DONE (Project: Webアプリケーション開発, SubProject: フロントエンド開発)
```

#### 3.3.2 deps graph（直接の先行・後続ノード）

```bash
$ pmtool deps graph task 6

Dependency Graph for Task 6: 商品一覧画面実装

Direct Predecessors:
  → Task 7: 商品API実装 [✓] DONE

Direct Successors:
  → Task 8: 商品詳細画面実装 [✓] DONE
```

#### 3.3.3 deps chain（依存チェーン）

```bash
$ pmtool deps chain task --from 7 --to 8

Dependency Chain: Task 7 → Task 8

Task 7: 商品API実装 [✓] DONE
  → Task 6: 商品一覧画面実装 [✓] DONE
    → Task 8: 商品詳細画面実装 [✓] DONE
```

#### 3.3.4 deps impact（影響範囲分析）

新しいTaskを追加して、影響範囲を確認します。

```bash
# 新しいTaskを追加
$ pmtool add task --subproject 1 --name "決済画面実装"
✓ Task created: 決済画面実装 (ID: 9)

$ pmtool add task --subproject 1 --name "注文完了画面実装"
✓ Task created: 注文完了画面実装 (ID: 10)

# 依存関係を追加
$ pmtool deps add task --from 8 --to 9
✓ Dependency added: Task 8 → Task 9

$ pmtool deps add task --from 8 --to 10
✓ Dependency added: Task 8 → Task 10
```

Task 8 を DONE にすると解放されるノードを確認します。

```bash
$ pmtool deps impact task 8

Impact Analysis for Task 8: 商品詳細画面実装

If Task 8 is marked as DONE, the following tasks can be marked as DONE:
  → Task 9: 決済画面実装 (if all other conditions are met)
  → Task 10: 注文完了画面実装 (if all other conditions are met)
```

### 3.4 橋渡し削除の実践

Task 6（商品一覧画面実装）を削除して、依存関係を橋渡しします。

**削除前の依存関係:**
```
Task 7 → Task 6 → Task 8
```

**削除後の依存関係（橋渡し）:**
```
Task 7 → Task 8
```

```bash
# Task 6 を橋渡し削除（dry-run で影響を確認）
$ pmtool delete task 6 --bridge --dry-run

[DRY RUN] Bridging dependencies before deletion:
  - Task 7 → Task 6 → Task 8
  After deletion: Task 7 → Task 8

Would delete: Task 6: 商品一覧画面実装

# 実際に削除
$ pmtool delete task 6 --bridge

⚠️  Bridging dependencies before deletion:
  - Task 7 → Task 6 → Task 8
  After deletion: Task 7 → Task 8

Proceed? [y/N]: y
✓ Task deleted with bridge (ID: 6)
```

依存関係が橋渡しされたことを確認します。

```bash
$ pmtool deps list task 8

Dependencies for Task 8: 商品詳細画面実装

Predecessors (先行ノード):
  Task 7: 商品API実装 [✓] DONE (Project: Webアプリケーション開発, SubProject: バックエンド開発)

Successors (後続ノード):
  Task 9: 決済画面実装 [ ] UNSET (Project: Webアプリケーション開発, SubProject: フロントエンド開発)
  Task 10: 注文完了画面実装 [ ] UNSET (Project: Webアプリケーション開発, SubProject: フロントエンド開発)
```

Task 6 が削除され、Task 7 → Task 8 の依存関係が維持されました。

---

## 4. まとめ

このチュートリアルでは、以下の内容を実践しました。

**シナリオ1: 基本的なプロジェクト管理**
- プロジェクト・SubProject・Task・SubTask の作成
- ステータス更新（UNSET → NOT_STARTED → IN_PROGRESS → DONE）
- エンティティの更新（名前・説明）
- 削除操作（通常削除、連鎖削除）

**シナリオ2: 依存関係管理**
- 依存関係の追加（predecessor → successor）
- DONE遷移条件の理解（先行ノードと子SubTaskがすべてDONE）
- 依存関係の可視化（list、graph、chain、impact）
- 橋渡し削除（依存関係を再接続しながら削除）

**次のステップ:**
- **FAQ.md**: よくある質問とトラブルシューティングを確認
- **USER_GUIDE.md**: コマンドリファレンスの詳細を確認

**実践のヒント:**
- 大規模プロジェクトでは、Project → SubProject → Task の階層を活用して段階的に分解
- 依存関係を使って、作業順序を明確にする
- dry-run を活用して、削除やステータス変更の影響を事前に確認
- doctor コマンドで定期的にデータ整合性をチェック

pmtool を使った効果的なプロジェクト管理をお楽しみください！
