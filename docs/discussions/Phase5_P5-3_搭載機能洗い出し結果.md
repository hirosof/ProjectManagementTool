Phase 5（Textual版）に関する機能洗い出し（Bスレッド）が完了しました。
現時点では設計や実装判断は行っておらず、
「今後検討対象となる論点の一覧」を整理した段階です。

主な検討対象は以下です。

- DB初期化の自動化（初回起動時に自動作成）
- DB切替機能（複数DBの使い分け）
- テンプレート機能
  - DB内にJSON（canonical）として保持する案
  - export / import 機能
  - import時のテンプレ名変更（衝突回避）
  - プレビュー機能は要件として検討対象
- タスク／サブタスクのメモ機能
  - 調査・思考メモ用途
  - Markdown対応の可能性あり（未確定）
- 依存（前後関係）とは別に、順序制約を持たない「関連リンク」機能
  - ラベル付与は段階案
- 外部ファイル添付
  - DB肥大化回避のため、外部ファイル参照方式を候補
- データ可搬性・バックアップ
- 検索・絞り込み
- 変更履歴（ログ）
- 一括操作

次フェーズでは、これらの論点について
Phase 5 に含めるもの／Phase 6 以降に回すものを分解・判断していく予定です。
現時点では実装や設計は進めないでください。
